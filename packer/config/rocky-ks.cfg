# Generated by Anaconda 34.25.1.14
# Generated by pykickstart v3.32
#version=RHEL9
# Use graphical install
#graphical
repo --name="minimal" --baseurl=file:///run/install/sources/mount-0000-cdrom/minimal

%addon com_redhat_kdump --enable --reserve-mb='auto'

%end

# Keyboard layouts
keyboard --xlayouts='us'
# System language
lang en_US.UTF-8

# Network information
network  --bootproto=dhcp --device=ens18 --ipv6=auto --activate
network  --hostname=packer-rocky.int.letnh.dev

# Use CDROM installation media
cdrom

%packages
@^minimal-environment
@guest-agents

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.4.0
ignoredisk --only-use=sda
# Partition clearing information
clearpart --none --initlabel
# Disk partitioning information
part /boot --fstype="xfs" --ondisk=sda --size=512 --label=boot
part pv.302 --fstype="lvmpv" --ondisk=sda --grow
volgroup vg0 --pesize=4096 pv.302
logvol / --fstype="xfs" --size=4092 --label="root" --name=root --vgname=vg0
logvol swap --fstype="swap" --size=1024 --name=swap --vgname=vg0
logvol /var --fstype="xfs" --size=2048 --label="var" --name=var --vgname=vg0
logvol /tmp --fstype="xfs" --size=2048 --label="tmp" --name=tmp --vgname=vg0
logvol /home --fstype="xfs" --size=2048 --label="home" --name=home --vgname=vg0
#logvol /usr --fstype="xfs" --size=2048 --label="usr" --name=usr --vgname=vg0

# System timezone
timezone Europe/Helsinki --utc

# Root password
rootpw --allow-ssh packer

reboot
